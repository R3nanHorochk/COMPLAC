cat << 'EOF' > teste.slac
globvars
  total is int;

func SOMA(A: int, B: int): int
start
  return A + B;
end

proc main()
locvars
  x, y is int;
start
  x << 10;
  y << 20;
  total << SOMA(x, y);
  echo("Resultado: ", total);
end
EOF